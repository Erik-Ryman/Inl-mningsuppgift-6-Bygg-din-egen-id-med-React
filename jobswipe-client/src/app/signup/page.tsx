import RegisterPrivateForm from "@/components/RegisterPrivateForm";
import { NextPage } from "next";

const RegisterForm: NextPage = () => {
	return <RegisterPrivateForm />;
};

export default RegisterForm;
